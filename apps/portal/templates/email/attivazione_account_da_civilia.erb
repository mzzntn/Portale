Gentile <%= utente.nome %> <%= utente.cognome %>,
il tuo account è stato attivato dall'amministratore. Puoi autenticarti al portale dalla pagina <%=Portal.http_s_url%> 
usando le seguenti credenziali:

Username: <%= username %>
Password: <%= password %>

Ti ricordiamo che la tua password può essere modificata in qualsiasi momento (dopo aver eseguito l'accesso con le tue credenziali, funzione 'I tuoi dati')

Servizi attivati:
	<% utente.servizi_privati.each do |servizio| %><%= servizio.nome %>
	<% end %>

Cordiali saluti,
<%= Spider.conf.get('ente.tipo') %>  <%= Spider.conf.get('ente.nome') %>